version: '3.8'

services:
  extract:
    build:
      context: .
      dockerfile: src/extract/Dockerfile
    container_name: extract_service
    command: python extract_data.py
    volumes:
      - ./src/extract:/app
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
    working_dir: /app

  prepare_data:
    build:
      context: .
      dockerfile: src/prepare_data/Dockerfile
    container_name: prepare_data_service
    command: python prepare_data.py
    volumes:
      - ./src/prepare_data:/app
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
    working_dir: /app
    depends_on:
      - extract

  train_model:
    build:
      context: .
      dockerfile: src/train_model/Dockerfile
    container_name: train_model_service
    command: python train_model.py
    volumes:
      - ./src/train_model:/app
      - ./data:/app/data
      - ./models:/app/models
      - ./config.yaml:/app/config.yaml
    working_dir: /app
    depends_on:
      - prepare_data
